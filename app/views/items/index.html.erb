<% @items.each do |item| %>
  <li> <%= link_to item.name, item_path(item) %><span class='add_to_cart_button'> <%= button_to "Add To Cart", item_path(item), class:'btn btn-primary' %></span></li>
<% end %>

